{
  "name": "version-sync",
  "version": "1.0.0",
  "description": "Automatically sync version and generate changelogs with Git tags.",
  "main": "src/index.mjs",
  "scripts": {
    "version-sync": "node src/index.mjs"
  },
  "bin": {
    "version-sync": "./src/index.mjs"
  },
  "keywords": ["versioning", "changelog", "git", "tags"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4",
    "dotenv": "^16.0.1"
  }
}
